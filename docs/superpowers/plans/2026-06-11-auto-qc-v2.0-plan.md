# auto-qc v2.0：多规则集 + 逐规则打标 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 auto-qc 升级为多规则集 + 逐规则打标模式，输出宽表报告

**Architecture:** 在 `rules/` 目录存放 JSON 格式的规则集文件，运行时按 `--rule-sets` 参数选择加载。打标阶段从"一批一次检查所有规则"改为"一批一次检查一条规则"，LLM 调用全并发。结果按规则 × 对话交叉合并为宽表。报告输出为每条对话一行、每个规则一列 + 打标详情汇总列 + 细化统计。

**Tech Stack:** Python 3.13+, openpyxl, openai SDK, pytest

---

## 文件结构总览

| 文件 | 变更类型 | 职责 |
|------|----------|------|
| `rules/` | **新增** | 规则集 JSON 文件目录 |
| `rules/auto-pi.json` | **新增** | 人文角度规则集（示例） |
| `rules/project-standards.json` | **新增** | 项目规范规则集（示例） |
| `src/auto_qc/domain/schemas.py` | **修改** | 新增 `RuleSet` 模型 |
| `src/auto_qc/domain/rules.py` | **修改** | 新增 `load_rule_sets()`，ID 重编码 |
| `src/auto_qc/domain/rules.py` | **修改** | `load_or_parse_rules()` 重命名为 `load_rule_sets_from_json()` |
| `src/auto_qc/domain/prompts.py` | **修改** | 新增 `build_single_rule_prompt()` |
| `src/auto_qc/framework/validator.py` | **修改** | 新增 `validate_single_rule_output()` |
| `src/auto_qc/framework/orchestrator.py` | **修改** | 重写 Step 4 为逐规则并发打标 + 结果合并 |
| `src/auto_qc/domain/report.py` | **重写** | 宽表输出 + 细化统计 |
| `src/auto_qc/cli.py` | **修改** | `--rule-sets` 替代 `--rules` / `--rules-name` |
| `SKILL.md` | **修改** | 更新安装/运行说明 |
| `templates/worker-prompt.md` | **修改** | 简化为单规则模板 |

---

### Task 1: 规则集数据模型

**Files:**
- Modify: `src/auto_qc/domain/schemas.py`
- Test: `tests/domain/test_schemas.py`

- [ ] **Step 1: 在 schemas.py 中新增 RuleSet 模型**

在 `Rule` 类之后添加：

```python
@dataclass
class RuleSet:
    """一个规则集：命名的规则集合"""
    name: str               # "auto-pi"，目录名，用于 CLI 引用
    display_name: str       # "人文角度"，用于报表表头
    description: str = ""   # 规则集说明
    rules: list[Rule] = field(default_factory=list)
```

- [ ] **Step 2: 运行测试确认不破坏现有逻辑**

Run: `uv run pytest tests/domain/test_schemas.py -v`
Expected: 6 passed

- [ ] **Step 3: 提交**

```bash
git add src/auto_qc/domain/schemas.py
git commit -m "feat: 新增 RuleSet 数据模型"
```

---

### Task 2: 规则集 JSON 文件 + 加载函数

**Files:**
- Create: `rules/auto-pi.json`
- Create: `rules/project-standards.json`
- Modify: `src/auto_qc/domain/rules.py`
- Test: `tests/domain/test_rules.py`

- [ ] **Step 1: 创建示例规则集 JSON 文件**

`rules/auto-pi.json`:
```json
{
  "name": "auto-pi",
  "display_name": "人文角度",
  "description": "从人文/拟人/真人客服角度评估对话质量",
  "rules": [
    {
      "rule_id": "R01",
      "name": "无视用户明确拒绝",
      "severity": "高",
      "description": "用户明确表示不考虑后，AI 未礼貌结束对话",
      "detection_logic": "检测用户发言包含明确拒绝关键词后，AI 下一条发言是否继续推进",
      "examples": ["用户: 嗯，暂时不考虑了啊 | AI: 好的，那您考虑一下什么时候需要"]
    },
    {
      "rule_id": "R02",
      "name": "答非所问",
      "severity": "高",
      "description": "AI 的回答与用户的问题无关",
      "detection_logic": "检查 AI 是否回避了用户的核心问题",
      "examples": ["用户: 这个多少钱? | AI: 您可以先了解一下功能"]
    },
    {
      "rule_id": "R03",
      "name": "过度追问",
      "severity": "中",
      "description": "AI 在用户未回应的情况下反复追问",
      "detection_logic": "检查 AI 是否在同一轮对话中连续追问 3 次以上",
      "examples": []
    },
    {
      "rule_id": "R04",
      "name": "语气生硬",
      "severity": "中",
      "description": "AI 的语气缺乏温度，不像真人客服",
      "detection_logic": "检查 AI 是否使用了过于生硬或模板化的表达",
      "examples": ["AI: 那你再考虑一下吧"]
    }
  ]
}
```

`rules/project-standards.json`:
```json
{
  "name": "project-standards",
  "display_name": "项目规范",
  "description": "项目特定的对话规范标准",
  "rules": [
    {
      "rule_id": "R01",
      "name": "未介绍产品亮点",
      "severity": "高",
      "description": "AI 未在开场白中介绍产品核心亮点",
      "detection_logic": "检查开场白是否包含产品核心卖点介绍",
      "examples": []
    },
    {
      "rule_id": "R02",
      "name": "未确认用户意向",
      "severity": "中",
      "description": "AI 未在对话中确认用户的真实意向",
      "detection_logic": "检查 AI 是否询问了用户的意向或需求",
      "examples": []
    },
    {
      "rule_id": "R03",
      "name": "未引导下一步",
      "severity": "低",
      "description": "对话结束时 AI 未引导用户进行下一步操作",
      "detection_logic": "检查对话结尾 AI 是否有明确的下一步引导",
      "examples": ["AI: 那您先忙着，有需要再联系"]
    }
  ]
}
```

- [ ] **Step 2: 在 rules.py 中新增 load_rule_sets 函数**

在 `rules.py` 末尾追加：

```python
def load_rule_sets(
    rule_set_names: list[str],
    rules_dir: str | None = None,
) -> list[RuleSet]:
    """
    从 rules/ 目录加载指定的规则集。

    规则集文件命名规则: {name}.json
    ID 自动重编码为 {name}_{原ID}（如 auto-pi_R01）避免跨规则集冲突。

    返回: list[RuleSet]，每个 RuleSet 的 rules 中的 ID 已重编码。
    """
    if rules_dir is None:
        rules_dir = str(Path(__file__).resolve().parent.parent.parent.parent / "rules")

    result = []
    for name in rule_set_names:
        file_path = Path(rules_dir) / f"{name}.json"
        if not file_path.exists():
            raise FileNotFoundError(f"规则集文件不存在: {file_path}")

        data = json.loads(file_path.read_text(encoding="utf-8"))
        rules = []
        for r in data.get("rules", []):
            original_id = r.get("rule_id", "")
            rule = Rule(
                rule_id=f"{name}_{original_id}",  # 重编码
                name=r.get("name", ""),
                severity=r.get("severity", ""),
                description=r.get("description", ""),
                detection_logic=r.get("detection_logic", ""),
                examples=r.get("examples", []),
            )
            rules.append(rule)

        result.append(RuleSet(
            name=data.get("name", name),
            display_name=data.get("display_name", name),
            description=data.get("description", ""),
            rules=rules,
        ))

    return result
```

- [ ] **Step 3: 为 load_rule_sets 写测试**

在 `tests/domain/test_rules.py` 末尾追加：

```python
def test_load_rule_sets_by_name():
    """按名称加载规则集，ID 自动重编码。"""
    from auto_qc.domain.rules import load_rule_sets
    rule_sets = load_rule_sets(["auto-pi"])
    assert len(rule_sets) == 1
    rs = rule_sets[0]
    assert rs.name == "auto-pi"
    assert len(rs.rules) >= 1
    # ID 应已被重编码
    assert rs.rules[0].rule_id.startswith("auto-pi_")


def test_load_rule_sets_multiple():
    """加载多个规则集，ID 应无冲突。"""
    from auto_qc.domain.rules import load_rule_sets
    rule_sets = load_rule_sets(["auto-pi", "project-standards"])
    assert len(rule_sets) == 2
    ids = []
    for rs in rule_sets:
        for r in rs.rules:
            ids.append(r.rule_id)
    assert len(ids) == len(set(ids)), "跨规则集 ID 应无重复"


def test_load_rule_sets_not_found():
    """不存在的规则集应抛出 FileNotFoundError。"""
    from auto_qc.domain.rules import load_rule_sets
    import pytest
    with pytest.raises(FileNotFoundError):
        load_rule_sets(["nonexistent"])
```

- [ ] **Step 4: 运行新测试**

Run: `uv run pytest tests/domain/test_rules.py::test_load_rule_sets_by_name tests/domain/test_rules.py::test_load_rule_sets_multiple tests/domain/test_rules.py::test_load_rule_sets_not_found -v`
Expected: 3 passed

- [ ] **Step 5: 提交**

```bash
git add rules/ src/auto_qc/domain/rules.py tests/domain/test_rules.py
git commit -m "feat: 规则集 JSON 文件 + load_rule_sets 函数"
```

---

### Task 3: 单规则 Prompt

**Files:**
- Modify: `templates/worker-prompt.md`
- Modify: `src/auto_qc/domain/prompts.py`
- Test: `tests/domain/test_prompts.py`

- [ ] **Step 1: 简化 worker-prompt.md 为单规则模板**

将 `templates/worker-prompt.md` 内容替换为：

```markdown
# 单规则质检任务

你是一名质检员。请检查以下对话是否违反指定的规则。

## 规则

{{RULE_JSON}}

## 对话数据

以下是你需要检查的 {{BATCH_SIZE}} 条对话：

{{CONVERSATIONS}}

## 工作要求

1. **逐条判断**：对每一条对话，判断是否违反上述规则。
2. **违规输出依据**：对于判定为违规的对话，必须附带证据（引用对话原文）。`evidence` 字段格式为 "用户: [用户原话] | AI: [AI原话]"。
3. **推理过程**：对于判定为违规的对话，需附带简短推理链说明为什么判定违规。

## 输出格式

必须输出严格的 JSON，格式如下：

```json
{
  "batch_id": {{BATCH_ID}},
  "rule_id": "{{RULE_ID}}",
  "results": [
    {
      "id": "对话ID",
      "violates": true,
      "evidence": "用户: XXX | AI: XXX",
      "reasoning": "简要推理过程"
    },
    {
      "id": "对话ID",
      "violates": false,
      "evidence": "",
      "reasoning": ""
    }
  ]
}
```

**重要**：
- `results` 必须包含批次中的每一条对话，一条不能少
- `violates` 为 `true` 时，`evidence` 和 `reasoning` 不可为空
- `violates` 为 `false` 时，`evidence` 和 `reasoning` 为空字符串
- 只输出 JSON，不要输出其他内容
```

- [ ] **Step 2: 在 prompts.py 中新增 build_single_rule_prompt 函数**

在 `prompts.py` 末尾追加：

```python
def build_single_rule_prompt(batch: Batch, rule: Rule) -> str:
    """
    组装单规则质检 prompt。
    每条规则独立构建一次 prompt，只包含一条规则的描述。
    """
    template = _load_template("worker-prompt.md")

    conversations_json = json.dumps(
        [{"id": c.id, "time": c.time, "intent": c.intent, "conversation": c.conversation}
         for c in batch.conversations],
        ensure_ascii=False,
        indent=2,
    )

    rule_json = json.dumps(
        {"rule_id": rule.rule_id, "name": rule.name, "severity": rule.severity,
         "description": rule.description, "detection_logic": rule.detection_logic,
         "examples": rule.examples},
        ensure_ascii=False,
        indent=2,
    )

    prompt = template.replace("{{RULE_JSON}}", rule_json)
    prompt = prompt.replace("{{RULE_ID}}", rule.rule_id)
    prompt = prompt.replace("{{BATCH_SIZE}}", str(batch.size))
    prompt = prompt.replace("{{CONVERSATIONS}}", conversations_json)
    prompt = prompt.replace("{{BATCH_ID}}", str(batch.batch_id))

    return prompt
```

同时更新导入，在文件顶部增加：

```python
from auto_qc.domain.schemas import RulePackage, Batch, Rule
```

- [ ] **Step 3: 为 build_single_rule_prompt 写测试**

在 `tests/domain/test_prompts.py` 末尾追加：

```python
def test_build_single_rule_prompt_contains_rule_id():
    """单规则 prompt 应包含指定的 rule_id。"""
    from auto_qc.domain.prompts import build_single_rule_prompt
    from auto_qc.domain.schemas import Rule, Batch, Conversation

    rule = Rule(rule_id="auto-pi_R01", name="答非所问", severity="高",
                description="AI 回答与问题无关", detection_logic="检查")
    batch = Batch(batch_id=1, conversations=[
        Conversation(id="001", time="", intent="", conversation="test"),
    ])
    prompt = build_single_rule_prompt(batch, rule)
    assert "auto-pi_R01" in prompt
    assert "答非所问" in prompt


def test_build_single_rule_prompt_only_one_rule():
    """单规则 prompt 中应只包含一条规则。"""
    from auto_qc.domain.prompts import build_single_rule_prompt
    from auto_qc.domain.schemas import Rule, Batch, Conversation

    rule = Rule(rule_id="R01", name="测试规则", severity="高",
                description="描述", detection_logic="逻辑")
    batch = Batch(batch_id=1, conversations=[
        Conversation(id="001", time="", intent="", conversation="test"),
    ])
    prompt = build_single_rule_prompt(batch, rule)
    # "规则"这个词应该只出现有限次数（标题一次、描述一次），不应该有数组格式的多规则列表
    assert prompt.count("rule_id") == 1  # 只包含一条规则的 rule_id
```

- [ ] **Step 4: 运行测试**

Run: `uv run pytest tests/domain/test_prompts.py -v`
Expected: 新增的 2 个测试通过

- [ ] **Step 5: 提交**

```bash
git add templates/worker-prompt.md src/auto_qc/domain/prompts.py tests/domain/test_prompts.py
git commit -m "feat: 单规则 prompt 模板 + build_single_rule_prompt"
```

---

### Task 4: 逐条校验

**Files:**
- Modify: `src/auto_qc/framework/validator.py`
- Test: `tests/framework/test_validator.py`

- [ ] **Step 1: 在 validator.py 中新增 validate_single_rule_output 函数**

在文件末尾追加：

```python
def validate_single_rule_output(
    raw_json: str,
    batch_size: int,
    expected_rule_id: str,
    expected_conv_ids: set[str],
) -> dict:
    """
    校验单规则 LLM 调用的返回结果。

    校验项：
    1. JSON 合法
    2. rule_id 匹配
    3. results 数量 == batch_size
    4. 每条结果有 id 且属于 expected_conv_ids
    5. 每条结果有 violates (true/false)
    6. violates=true 时 evidence 和 reasoning 非空

    所有校验项失败均触发整批重试（抛出 ValidationError）。
    """
    try:
        data = json.loads(raw_json)
    except json.JSONDecodeError as e:
        raise ValidationError(f"单规则输出 JSON 解析失败: {e}")

    actual_rule_id = data.get("rule_id", "")
    if actual_rule_id != expected_rule_id:
        raise ValidationError(
            f"rule_id 不匹配: 期望 {expected_rule_id}，实际 {actual_rule_id}"
        )

    results = data.get("results", [])
    if len(results) != batch_size:
        raise ValidationError(
            f"结果数量不匹配: 期望 {batch_size} 条，实际 {len(results)} 条"
        )

    seen_ids = set()
    for r in results:
        rid = r.get("id", "")
        if not rid:
            raise ValidationError("存在空 ID 的结果")
        if rid in seen_ids:
            raise ValidationError(f"存在重复 ID: {rid}")
        if rid not in expected_conv_ids:
            raise ValidationError(f"ID {rid} 不在期望的对话 ID 集合中")
        seen_ids.add(rid)

        violates = r.get("violates")
        if violates not in (True, False):
            raise ValidationError(f"ID {rid}: violates 值无效 ({violates})")

        if violates:
            if not r.get("evidence", "").strip():
                raise ValidationError(f"ID {rid}: violates=true 但 evidence 为空")
            if not r.get("reasoning", "").strip():
                raise ValidationError(f"ID {rid}: violates=true 但 reasoning 为空")

    return data
```

- [ ] **Step 2: 为 validate_single_rule_output 写测试**

在 `tests/framework/test_validator.py` 末尾追加：

```python
class TestValidateSingleRuleOutput:
    def test_valid_output(self):
        raw = json.dumps({
            "batch_id": 1,
            "rule_id": "auto-pi_R01",
            "results": [
                {"id": "001", "violates": True, "evidence": "用户: X | AI: Y", "reasoning": "违规"},
                {"id": "002", "violates": False, "evidence": "", "reasoning": ""},
            ],
        })
        data = validate_single_rule_output(raw, 2, "auto-pi_R01", {"001", "002"})
        assert data["rule_id"] == "auto-pi_R01"
        assert len(data["results"]) == 2

    def test_invalid_json_raises(self):
        with pytest.raises(ValidationError, match="JSON"):
            validate_single_rule_output("not json", 1, "R01", {"001"})

    def test_rule_id_mismatch_raises(self):
        raw = json.dumps({
            "batch_id": 1, "rule_id": "wrong",
            "results": [{"id": "001", "violates": False, "evidence": "", "reasoning": ""}],
        })
        with pytest.raises(ValidationError, match="rule_id"):
            validate_single_rule_output(raw, 1, "expected", {"001"})

    def test_count_mismatch_raises(self):
        raw = json.dumps({
            "batch_id": 1, "rule_id": "R01",
            "results": [{"id": "001", "violates": False, "evidence": "", "reasoning": ""}],
        })
        with pytest.raises(ValidationError, match="数量不匹配"):
            validate_single_rule_output(raw, 3, "R01", {"001"})

    def test_missing_evidence_raises(self):
        raw = json.dumps({
            "batch_id": 1, "rule_id": "R01",
            "results": [
                {"id": "001", "violates": True, "evidence": "", "reasoning": ""},
            ],
        })
        with pytest.raises(ValidationError, match="evidence"):
            validate_single_rule_output(raw, 1, "R01", {"001"})

    def test_missing_reasoning_raises(self):
        raw = json.dumps({
            "batch_id": 1, "rule_id": "R01",
            "results": [
                {"id": "001", "violates": True, "evidence": "证据", "reasoning": ""},
            ],
        })
        with pytest.raises(ValidationError, match="reasoning"):
            validate_single_rule_output(raw, 1, "R01", {"001"})

    def test_invalid_violates_raises(self):
        raw = json.dumps({
            "batch_id": 1, "rule_id": "R01",
            "results": [
                {"id": "001", "violates": "yes", "evidence": "", "reasoning": ""},
            ],
        })
        with pytest.raises(ValidationError, match="violates"):
            validate_single_rule_output(raw, 1, "R01", {"001"})

    def test_unexpected_id_raises(self):
        raw = json.dumps({
            "batch_id": 1, "rule_id": "R01",
            "results": [
                {"id": "999", "violates": False, "evidence": "", "reasoning": ""},
            ],
        })
        with pytest.raises(ValidationError, match="999"):
            validate_single_rule_output(raw, 1, "R01", {"001"})
```

- [ ] **Step 3: 运行测试**

Run: `uv run pytest tests/framework/test_validator.py -v`
Expected: 新增的 7 个测试通过（加上原来的 6 个 = 13 个）

- [ ] **Step 4: 提交**

```bash
git add src/auto_qc/framework/validator.py tests/framework/test_validator.py
git commit -m "feat: 逐条合规校验 validate_single_rule_output"
```

---

### Task 5: 逐规则并发打标 — orchestrator Step 4 重写

**Files:**
- Modify: `src/auto_qc/framework/orchestrator.py`
- Modify: `src/auto_qc/domain/rules.py`

- [ ] **Step 1: 新增结果合并辅助函数 merge_to_wide_rows**

在新的 `src/auto_qc/domain/merger.py` 文件中实现：

```python
"""逐规则打标结果合并为宽表"""
from typing import Any


def merge_to_wide_rows(
    per_rule_results: dict[str, dict[str, dict]],
    conversations: list[dict],
    rule_names: dict[str, str],
) -> list[dict]:
    """
    将逐规则打标结果合并为宽表行。

    per_rule_results: {
        "auto-pi_R01": {
            "001": {"violates": True, "evidence": "..."},
            ...
        },
        ...
    }

    conversations: [{"id": "001", "time": "...", "intent": "..."}, ...]

    返回宽表行，每条对话一行:
    [{
        "id": "001",
        "time": "...",
        "intent": "...",
        "rules": {
            "auto-pi_R01": {"result": "违规", "evidence": "...", "rule_name": "答非所问"},
            ...
        },
        "summary": "答非所问：证据内容\n语气生硬：证据内容"
    }, ...]
    """
    wide_rows = []
    for conv in conversations:
        cid = conv["id"]
        rules_result = {}
        for rule_id, conv_results in per_rule_results.items():
            result = conv_results.get(cid, {})
            is_violation = result.get("violates", False)
            rules_result[rule_id] = {
                "result": "违规" if is_violation else "通过",
                "evidence": result.get("evidence", ""),
                "rule_name": rule_names.get(rule_id, rule_id),
            }

        # 构建打标详情汇总
        summary_lines = []
        for rule_id, rr in rules_result.items():
            if rr["result"] == "违规":
                summary_lines.append(f"{rr['rule_name']}：{rr['evidence']}")
        summary = "\n".join(summary_lines)

        wide_rows.append({
            "id": cid,
            "time": conv.get("time", ""),
            "intent": conv.get("intent", ""),
            "rules": rules_result,
            "summary": summary,
        })

    return wide_rows
```

- [ ] **Step 2: 重写 orchestrator 的 run_qc 函数**

核心改动：
1. Step 2: 改用 `load_rule_sets()` 加载规则，不再用 `load_or_parse_rules()`
2. Step 4: 改为逐规则并发打标
3. Step 5: 交叉验证适配逐规则模式
4. Step 6: 统计计算
5. Step 7: 报告生成

在 `orchestrator.py` 开头新增导入：

```python
from auto_qc.domain.rules import load_rule_sets
from auto_qc.domain.prompts import build_single_rule_prompt
from auto_qc.framework.validator import validate_single_rule_output
from auto_qc.domain.merger import merge_to_wide_rows
```

修改 `run_qc` 函数的 Step 2：

```python
    # ─── Step 2: 规则集加载 + 校验 ───
    print("[Step 2] 规则集加载 + 校验...")
    rule_sets = load_rule_sets(rule_set_names)  # rule_set_names 从 CLI 传入
    all_rules = []  # 所有规则集的所有规则打平
    rule_name_map = {}  # rule_id → rule_name
    rule_set_map = {}  # rule_id → rule_set_name
    for rs in rule_sets:
        for r in rs.rules:
            all_rules.append(r)
            rule_name_map[r.rule_id] = r.name
            rule_set_map[r.rule_id] = rs.name
    print(f"  [OK] 加载 {len(rule_sets)} 个规则集, {len(all_rules)} 条规则")
```

修改 Step 4 为逐规则并发打标：

```python
    # ─── Step 4: 逐规则并发打标 ───
    print("[Step 4] 逐规则并发打标...")
    coordinator = Coordinator(work_dir)
    create_progress(work_dir, len(batches), phase="qc")

    # 每批次的对话 ID 集合
    batch_conv_ids = {b.batch_id: set(b.ids) for b in batches}
    # 对话基本信息（id/time/intent）
    all_convs = []
    for b in batches:
        for c in b.conversations:
            all_convs.append({"id": c.id, "time": c.time, "intent": c.intent})

    sem = asyncio.Semaphore(50)

    per_rule_results: dict[str, dict[str, dict]] = {}

    async def _run_single_rule(
        rule: Rule,
        batches: list[Batch],
    ) -> dict[str, dict[str, dict]]:
        """对一条规则的所有批次并发打标。"""
        rule_conv_results = {}

        async def _check_batch(batch: Batch) -> list[dict]:
            for attempt in range(3):
                try:
                    prompt = build_single_rule_prompt(batch, rule)
                    raw = await call_llm_with_retry(prompt)
                    json_text = extract_json(raw)
                    data = validate_single_rule_output(
                        json_text, batch.size, rule.rule_id, batch_conv_ids[batch.batch_id],
                    )
                    coordinator.mark_done(batch.batch_id)
                    return data["results"]
                except Exception as e:
                    retries = coordinator.increment_retry(batch.batch_id)
                    if retries >= 3:
                        coordinator.mark_failed(batch.batch_id)
                        print(f"  {rule.rule_id} 批次 {batch.batch_id} 失败: {e}")
                        # 返回空结果
                        return [{"id": cid, "violates": False, "evidence": "", "reasoning": ""}
                                for cid in batch_conv_ids[batch.batch_id]]
                    if attempt < 2:
                        print(f"  {rule.rule_id} 批次 {batch.batch_id} 第 {attempt+1} 次重试: {e}")
            return []

        tasks = [_check_batch(b) for b in batches]
        all_batch_results = await asyncio.gather(*tasks)

        for batch_results in all_batch_results:
            for r in batch_results:
                rule_conv_results[r["id"]] = {
                    "violates": r.get("violates", False),
                    "evidence": r.get("evidence", ""),
                }
        return rule_conv_results

    # 对所有规则并发执行
    rule_tasks = [_run_single_rule(r, batches) for r in all_rules]
    all_results_list = await asyncio.gather(*rule_tasks)

    for i, rule in enumerate(all_rules):
        per_rule_results[rule.rule_id] = all_results_list[i]

    # 合并为宽表
    wide_rows = merge_to_wide_rows(per_rule_results, all_convs, rule_name_map)
    print(f"  [OK] 逐规则打标完成: {len(all_rules)} 条规则 × {len(batches)} 批 = {len(all_rules) * len(batches)} 次调用")

    # ─── Step 5: 交叉验证（适配逐规则模式）───
    print("[Step 5] 交叉验证...")
    sample = fixed_sample(wide_rows, sample_size=200)
    if sample:
        recheck_results = []
        sample_ids = {s["id"] for s in sample}
        sample_convs = [c for c in all_convs if c["id"] in sample_ids]
        sample_batch = Batch(batch_id=999, conversations=[
            Conversation(id=c["id"], time=c["time"], intent=c["intent"],
                         conversation=conv_text_map.get(c["id"], ""))
            for c in sample_convs
        ])

        for rule in all_rules:
            for attempt in range(3):
                try:
                    prompt = build_single_rule_prompt(sample_batch, rule)
                    raw = await call_llm_with_retry(prompt)
                    json_text = extract_json(raw)
                    data = validate_single_rule_output(
                        json_text, sample_batch.size, rule.rule_id, sample_ids,
                    )
                    for r in data["results"]:
                        recheck_results.append({
                            "id": r["id"],
                            "rule_id": rule.rule_id,
                            "violates": r["violates"],
                        })
                    break
                except Exception as e:
                    if attempt >= 2:
                        print(f"  交叉验证 {rule.rule_id} 失败: {e}")
                        break

        # 对比并计算 Kappa（复用现有 compare_results，需适配规则级对比）
        cross_result = _compare_cross_results(sample, recheck_results)
        print(f"  [OK] 交叉验证: 抽检 {len(sample)} 条")
        print(f"     总体 Kappa={cross_result.kappa:.3f} ({cross_result.kappa_status})")

    # ─── Step 6: 统计计算 ───
```

更新统计计算函数 `_compute_stats`：

```python
def _compute_stats_v2(wide_rows: list[dict], rule_sets: list, rule_set_map: dict) -> dict:
    """v2.0 统计概览计算。"""
    total = len(wide_rows)
    violation_rows = [r for r in wide_rows if r["summary"]]
    pass_rows = [r for r in wide_rows if not r["summary"]]

    # 规则集统计
    rule_set_stats = {}
    for rs in rule_sets:
        rs_rules = [r.rule_id for r in rs.rules]
        total_checks = total * len(rs_rules)
        violations = sum(
            1 for row in wide_rows
            for rid in rs_rules
            if row["rules"].get(rid, {}).get("result") == "违规"
        )
        rule_set_stats[rs.name] = {
            "total_checks": total_checks,
            "violations": violations,
            "rate": f"{violations / total_checks * 100:.1f}%" if total_checks else "0%",
        }

    # 规则统计
    rule_stats = {}
    for row in wide_rows:
        for rid, rr in row["rules"].items():
            if rid not in rule_stats:
                rule_stats[rid] = {"pass": 0, "violation": 0}
            if rr["result"] == "违规":
                rule_stats[rid]["violation"] += 1
            else:
                rule_stats[rid]["pass"] += 1

    for rid, s in rule_stats.items():
        s["pass_rate"] = f"{s['pass'] / total * 100:.1f}%"
        s["violation_rate"] = f"{s['violation'] / total * 100:.1f}%"

    # 问题分布
    problem_distribution = []
    for rid, s in rule_stats.items():
        if s["violation"] > 0:
            problem_distribution.append({
                "rule_id": rid,
                "rule_name": rule_name_map.get(rid, rid),
                "count": s["violation"],
                "ratio": f"{s['violation'] / len(violation_rows) * 100:.1f}%" if violation_rows else "0%",
            })
    problem_distribution.sort(key=lambda x: x["count"], reverse=True)

    return {
        "total": total,
        "pass_count": len(pass_rows),
        "violation_count": len(violation_rows),
        "violation_rate": f"{len(violation_rows) / total * 100:.1f}%" if total else "0%",
        "rule_set_stats": rule_set_stats,
        "rule_stats": rule_stats,
        "problem_distribution": problem_distribution,
        "rule_name_map": rule_name_map,
    }


def _compare_cross_results(
    sample_wide: list[dict],
    recheck_results: list[dict],
) -> CrossValidationResult:
    """
    适配 v2.0 交叉验证：将宽表行 + 逐规则复检结果
    转为 compare_results 可接受的格式。
    """
    # 构建 original 格式
    original = []
    for row in sample_wide:
        violations = []
        for rid, rr in row["rules"].items():
            if rr["result"] == "违规":
                violations.append({"rule_id": rid})
        original.append({"id": row["id"], "violations": violations})

    # 构建 recheck 格式
    recheck_map = {}
    for r in recheck_results:
        cid = r["id"]
        if cid not in recheck_map:
            recheck_map[cid] = {"id": cid, "violations": []}
        if r["violates"]:
            recheck_map[cid]["violations"].append({"rule_id": r["rule_id"]})

    return compare_results(original, list(recheck_map.values()))
```
```

- [ ] **Step 3: 提交**

```bash
git add src/auto_qc/domain/merger.py src/auto_qc/framework/orchestrator.py
git commit -m "feat: 逐规则并发打标 + 结果合并"
```

---

### Task 6: 宽表报告

**Files:**
- Rewrite: `src/auto_qc/domain/report.py`
- Test: `tests/domain/test_report.py`

- [ ] **Step 1: 重写 report.py**

完整的宽表 + 细化统计报告生成：

```python
"""质检报告 Excel 生成（v2.0 宽表模式）"""
from pathlib import Path
import openpyxl
from openpyxl.styles import Font, PatternFill, Alignment

HEADER_FONT = Font(name="Microsoft YaHei", bold=True, size=11, color="FFFFFF")
HEADER_FILL = PatternFill(start_color="4472C4", end_color="4472C4", fill_type="solid")
SEVERITY_FILLS = {
    "高": PatternFill(start_color="FF6B6B", end_color="FF6B6B", fill_type="solid"),
    "中": PatternFill(start_color="FFD93D", end_color="FFD93D", fill_type="solid"),
    "低": PatternFill(start_color="6BCB77", end_color="6BCB77", fill_type="solid"),
}
PASS_FILL = PatternFill(start_color="E8F5E9", end_color="E8F5E9", fill_type="solid")
VIOLATION_FILL = PatternFill(start_color="FFEBEE", end_color="FFEBEE", fill_type="solid")


def _style_header(cell):
    cell.font = HEADER_FONT
    cell.fill = HEADER_FILL
    cell.alignment = Alignment(horizontal="center", vertical="center", wrap_text=True)


def write_report(output_path: str, wide_rows: list[dict], stats: dict) -> None:
    """生成宽表质检报告 Excel。"""
    wb = openpyxl.Workbook()

    _write_detail_sheet(wb, wide_rows, stats)
    _write_stats_sheet(wb, stats)

    Path(output_path).parent.mkdir(parents=True, exist_ok=True)
    wb.save(output_path)
    wb.close()


def _write_detail_sheet(wb, wide_rows: list[dict], stats: dict) -> None:
    """Sheet 1: 打标明细（宽表）。"""
    ws = wb.active
    ws.title = "打标明细"
    ws.sheet_properties.tabColor = "4472C4"

    # 收集所有规则列（保持顺序）
    rule_ids = []
    rule_names = {}
    if wide_rows:
        rule_ids = list(wide_rows[0]["rules"].keys())
        for rid in rule_ids:
            rule_names[rid] = wide_rows[0]["rules"][rid].get("rule_name", rid)

    headers = ["id", "时间", "意向结果"] + [f"{rid}: {rule_names.get(rid, rid)}" for rid in rule_ids] + ["打标详情"]
    for col, h in enumerate(headers, 1):
        _style_header(ws.cell(1, col, h))

    for row_idx, row in enumerate(wide_rows, 2):
        ws.cell(row_idx, 1, row["id"])
        ws.cell(row_idx, 2, row.get("time", ""))
        ws.cell(row_idx, 3, row.get("intent", ""))

        for col_idx, rid in enumerate(rule_ids, 4):
            rr = row["rules"].get(rid, {})
            result = rr.get("result", "通过")
            cell = ws.cell(row_idx, col_idx, result)
            if result == "违规":
                cell.fill = VIOLATION_FILL
            else:
                cell.fill = PASS_FILL

        summary_col = 4 + len(rule_ids)
        ws.cell(row_idx, summary_col, row.get("summary", ""))

    # 自动调整列宽
    ws.column_dimensions["A"].width = 12
    ws.column_dimensions["B"].width = 16
    ws.column_dimensions["C"].width = 10
    for col_idx in range(4, 4 + len(rule_ids)):
        ws.column_dimensions[openpyxl.utils.get_column_letter(col_idx)].width = 14
    ws.column_dimensions[openpyxl.utils.get_column_letter(4 + len(rule_ids))].width = 60


def _write_stats_sheet(wb, stats: dict) -> None:
    """Sheet 2: 统计概览。"""
    ws = wb.create_sheet("统计概览")
    ws.sheet_properties.tabColor = "FFD93D"

    row = 1

    # 总体概览
    ws.cell(row, 1, "【总体概览】")
    ws.cell(row, 1).font = Font(bold=True, size=13)
    row += 1

    for label, key in [("总对话数", "total"), ("违规对话数", "violation_count"),
                        ("通过对话数", "pass_count"), ("总体违规率", "violation_rate")]:
        val = stats.get(key, 0)
        ws.cell(row, 1, label)
        ws.cell(row, 2, val)
        row += 1

    row += 1

    # 按规则集统计
    ws.cell(row, 1, "【按规则集统计】")
    ws.cell(row, 1).font = Font(bold=True, size=13)
    row += 1

    for col, h in enumerate(["规则集", "总检查次数", "违规次数", "违规率"], 1):
        _style_header(ws.cell(row, col, h))
    row += 1

    for rs_name, rs_stat in stats.get("rule_set_stats", {}).items():
        ws.cell(row, 1, rs_name)
        ws.cell(row, 2, rs_stat.get("total_checks", 0))
        ws.cell(row, 3, rs_stat.get("violations", 0))
        ws.cell(row, 4, rs_stat.get("rate", "0%"))
        row += 1

    row += 1

    # 按规则统计
    ws.cell(row, 1, "【按规则统计】")
    ws.cell(row, 1).font = Font(bold=True, size=13)
    row += 1

    for col, h in enumerate(["规则ID", "规则名称", "规则集", "通过数", "违规数", "通过率", "违规率"], 1):
        _style_header(ws.cell(row, col, h))
    row += 1

    rule_name_map = stats.get("rule_name_map", {})
    for rid in sorted(stats.get("rule_stats", {}).keys()):
        s = stats["rule_stats"][rid]
        ws.cell(row, 1, rid)
        ws.cell(row, 2, rule_name_map.get(rid, rid))
        # 从 rid 推断规则集名（auto-pi_R01 → auto-pi）
        rs_name = rid.split("_")[0] if "_" in rid else ""
        ws.cell(row, 3, rs_name)
        ws.cell(row, 4, s["pass"])
        ws.cell(row, 5, s["violation"])
        ws.cell(row, 6, s.get("pass_rate", ""))
        ws.cell(row, 7, s.get("violation_rate", ""))
        row += 1

    row += 1

    # 问题分布
    ws.cell(row, 1, "【有违规case中的问题分布】")
    ws.cell(row, 1).font = Font(bold=True, size=13)
    row += 1
    ws.cell(row, 1, f"（在 {stats.get('violation_count', 0)} 个至少有一条违规的对话中）")
    row += 1

    for col, h in enumerate(["问题类型", "出现次数", "占违规case比例"], 1):
        _style_header(ws.cell(row, col, h))
    row += 1

    for pd in stats.get("problem_distribution", []):
        ws.cell(row, 1, f"{pd['rule_id']}: {pd['rule_name']}")
        ws.cell(row, 2, pd["count"])
        ws.cell(row, 3, pd["ratio"])
        row += 1

    ws.column_dimensions["A"].width = 30
    ws.column_dimensions["B"].width = 15
    ws.column_dimensions["C"].width = 15
    for col in ["D", "E", "F", "G"]:
        ws.column_dimensions[col].width = 12


def verify_report_exists(output_path: str) -> bool:
    """验证报告文件是否生成且非空。"""
    p = Path(output_path)
    return p.exists() and p.stat().st_size > 0
```

- [ ] **Step 2: 写报告测试**

`tests/domain/test_report.py` 内容替换为：

```python
import pytest
from auto_qc.domain.report import write_report, verify_report_exists
import json
import tempfile
from pathlib import Path


def _make_sample_inputs():
    rule_name_map = {
        "auto-pi_R01": "答非所问",
        "auto-pi_R02": "过度追问",
        "project_R01": "未介绍产品",
    }
    wide_rows = [
        {
            "id": "001", "time": "06-01", "intent": "B",
            "rules": {
                "auto-pi_R01": {"result": "违规", "evidence": "用户问XX AI说YY", "rule_name": "答非所问"},
                "auto-pi_R02": {"result": "通过", "evidence": "", "rule_name": "过度追问"},
                "project_R01": {"result": "违规", "evidence": "AI未介绍产品", "rule_name": "未介绍产品"},
            },
            "summary": "答非所问：用户问XX AI说YY\n未介绍产品：AI未介绍产品",
        },
        {
            "id": "002", "time": "06-01", "intent": "A",
            "rules": {
                "auto-pi_R01": {"result": "通过", "evidence": "", "rule_name": "答非所问"},
                "auto-pi_R02": {"result": "通过", "evidence": "", "rule_name": "过度追问"},
                "project_R01": {"result": "通过", "evidence": "", "rule_name": "未介绍产品"},
            },
            "summary": "",
        },
    ]
    stats = {
        "total": 2,
        "pass_count": 1,
        "violation_count": 1,
        "violation_rate": "50.0%",
        "rule_set_stats": {
            "auto-pi": {"total_checks": 4, "violations": 1, "rate": "25.0%"},
            "project": {"total_checks": 2, "violations": 1, "rate": "50.0%"},
        },
        "rule_stats": {
            "auto-pi_R01": {"pass": 1, "violation": 1, "pass_rate": "50.0%", "violation_rate": "50.0%"},
            "auto-pi_R02": {"pass": 2, "violation": 0, "pass_rate": "100.0%", "violation_rate": "0.0%"},
            "project_R01": {"pass": 1, "violation": 1, "pass_rate": "50.0%", "violation_rate": "50.0%"},
        },
        "problem_distribution": [
            {"rule_id": "auto-pi_R01", "rule_name": "答非所问", "count": 1, "ratio": "100.0%"},
            {"rule_id": "project_R01", "rule_name": "未介绍产品", "count": 1, "ratio": "100.0%"},
        ],
        "rule_name_map": rule_name_map,
    }
    return wide_rows, stats


def test_write_report_creates_file():
    with tempfile.TemporaryDirectory() as tmp:
        output = str(Path(tmp) / "report.xlsx")
        wide_rows, stats = _make_sample_inputs()
        write_report(output, wide_rows, stats)
        assert verify_report_exists(output)


def test_report_has_two_sheets():
    import openpyxl
    with tempfile.TemporaryDirectory() as tmp:
        output = str(Path(tmp) / "report.xlsx")
        wide_rows, stats = _make_sample_inputs()
        write_report(output, wide_rows, stats)
        wb = openpyxl.load_workbook(output)
        assert "打标明细" in wb.sheetnames
        assert "统计概览" in wb.sheetnames


def test_report_wide_table_headers():
    import openpyxl
    with tempfile.TemporaryDirectory() as tmp:
        output = str(Path(tmp) / "report.xlsx")
        wide_rows, stats = _make_sample_inputs()
        write_report(output, wide_rows, stats)
        wb = openpyxl.load_workbook(output)
        ws = wb["打标明细"]
        # id | 时间 | 意向结果 | 3条规则列 | 打标详情 = 7列
        assert ws.max_column == 7
        # 001 的违规所在行应有 "答非所问" 在打标详情列
        summary_cell = ws.cell(2, 7).value
        assert "答非所问" in summary_cell


def test_report_stats_section_count():
    import openpyxl
    with tempfile.TemporaryDirectory() as tmp:
        output = str(Path(tmp) / "report.xlsx")
        wide_rows, stats = _make_sample_inputs()
        write_report(output, wide_rows, stats)
        wb = openpyxl.load_workbook(output)
        ws = wb["统计概览"]
        # 至少包含总体概览文本
        texts = [ws.cell(r, 1).value for r in range(1, ws.max_row + 1)]
        assert any("总体概览" in str(t) for t in texts if t)
        assert any("规则集统计" in str(t) for t in texts if t)
        assert any("按规则统计" in str(t) for t in texts if t)
```

- [ ] **Step 3: 运行测试**

Run: `uv run pytest tests/domain/test_report.py -v`
Expected: 4 passed

- [ ] **Step 4: 提交**

```bash
git add src/auto_qc/domain/report.py tests/domain/test_report.py
git commit -m "feat: 宽表报告 + 细化统计"
```

---

### Task 7: CLI + SKILL.md 更新

**Files:**
- Modify: `src/auto_qc/cli.py`
- Modify: `SKILL.md`

- [ ] **Step 1: 更新 CLI**

`src/auto_qc/cli.py`:

```python
"""CLI 命令行入口"""
import argparse
import asyncio
import datetime
from pathlib import Path


def main():
    parser = argparse.ArgumentParser(
        description="auto-qc 外呼通话文本质检",
        prog="auto-qc",
    )
    parser.add_argument("--data", required=True, help="源数据 Excel 文件路径")
    parser.add_argument("--rule-sets", required=True,
                        help="规则集名称，多个用逗号分隔（如 auto-pi,project-standards）")
    parser.add_argument("--output", help="报告输出路径（默认 output/<timestamp>_<run_name>/ 目录下）")
    parser.add_argument("--work-dir", help="工作目录（默认 output/<timestamp>_<run_name>/）")
    parser.add_argument("--run-name", help="运行名称（默认从数据文件名推断）")

    args = parser.parse_args()
    rule_set_names = [s.strip() for s in args.rule_sets.split(",") if s.strip()]
    if not rule_set_names:
        parser.error("--rule-sets 至少需要指定一个规则集")

    timestamp = datetime.datetime.now().strftime("%Y%m%d_%H%M%S")
    run_name = args.run_name or Path(args.data).stem
    work_dir = args.work_dir or f"output/{timestamp}_{run_name}"
    output_path = args.output or f"{work_dir}/质检报告_{timestamp}.xlsx"

    from auto_qc.framework.orchestrator import run_qc
    asyncio.run(run_qc(
        data_path=args.data,
        rule_set_names=rule_set_names,
        output_path=output_path,
        work_dir=work_dir,
    ))


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: 更新 SKILL.md**

`SKILL.md` 内容替换为：

```markdown
---
name: auto-qc
description: 外呼通话对话文本质检 v2.0。指定数据 Excel 和多规则集，自动完成逐规则打标，输出宽表 Excel 报告。
---

# auto-qc v2.0：多规则集 + 逐规则打标

## 触发方式

用户输入：`/auto-qc --data <excel路径> --rule-sets auto-pi,project-standards [--output <报告路径>]`

## 安装

Skill 安装后，agent 在 skill 目录引导执行首次依赖安装：

```bash
cd ~/.claude/skills/auto-qc/
uv sync
```

## 执行方式

```bash
cd ~/.claude/skills/auto-qc/
uv run -m auto_qc.cli \
  --data /path/to/data.xlsx \
  --rule-sets auto-pi,project-standards \
  --output ./质检报告.xlsx
```

说明：
- 代码不复制到终端目录，直接在 skill 目录运行
- 输出默认写到终端当前目录（`--output` 可指定绝对路径）
- 内置规则集：`auto-pi`（人文角度）、`project-standards`（项目规范）
- `--rule-sets` 支持多个逗号分隔，规则集定义在 `rules/` 目录下
```

- [ ] **Step 3: 提交**

```bash
git add src/auto_qc/cli.py SKILL.md
git commit -m "feat: CLI 更新 + SKILL.md v2.0"
```

---

### 最终验证

全量测试：
```bash
uv run pytest tests/ -v
```
Expected: 所有测试通过

手动验证流程：
```bash
uv run -m auto_qc.cli --data /path/to/sample.xlsx --rule-sets auto-pi --output ./test_report.xlsx
```
Expected: 生成宽表 Excel 报告
